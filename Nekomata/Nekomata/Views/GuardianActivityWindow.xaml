<Window x:Class="Nekomata.UI.Views.GuardianActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Guardian Activity" Width="980" Height="720" MinWidth="760" MinHeight="520"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource NekoBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid>
            <StackPanel>
                <TextBlock Text="GUARDIAN ACTIVITY" Style="{DynamicResource NekoTitleText}"/>
                <TextBlock Text="Persistent decisions, evidence and conflict-aware undo" Style="{DynamicResource NekoMutedText}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Content="REFRESH" Command="{Binding LoadCommand}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
        <Border Grid.Row="1" Margin="0,16,0,14" Padding="12" CornerRadius="10" Background="{DynamicResource NekoSurfaceAltBrush}">
            <TextBlock Text="{Binding Status}" Foreground="{DynamicResource NekoMutedTextBrush}" TextWrapping="Wrap"/>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,12" Padding="16" CornerRadius="14" Background="{DynamicResource NekoCardGradientBrush}" BorderBrush="{DynamicResource NekoBorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EntityType}" Foreground="{DynamicResource NekoAccentBrush}" FontWeight="Bold"/>
                                        <TextBlock Text="  ·  "/><TextBlock Text="{Binding Operation}"/>
                                        <TextBlock Text="  ·  batch "/><TextBlock Text="{Binding BatchLabel}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Description}" Foreground="{DynamicResource NekoMutedTextBrush}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Reason, StringFormat=Why: {0}}" Margin="0,9,0,0" TextWrapping="Wrap"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding AppliedAt, StringFormat={}{0:ddd dd MMM HH:mm}}" Foreground="{DynamicResource NekoMutedTextBrush}"/>
                                        <TextBlock Text="  ·  "/><TextBlock Text="{Binding ConfidenceLabel}"/>
                                        <TextBlock Text="  ·  "/><TextBlock Text="{Binding Status}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding UndoMessage}" Foreground="{DynamicResource NekoWarningBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="UNDO BATCH" Command="{Binding DataContext.UndoBatchCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" IsEnabled="{Binding CanUndo}" VerticalAlignment="Top" Margin="16,0,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
